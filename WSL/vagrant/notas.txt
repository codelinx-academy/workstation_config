01. criar pasta "Vagrant" em documentos
02. adicionar no final do .zshrc:
# Environment Variable for use VirtualBox on Windows Host (Vagrant)
export VAGRANT_WSL_ENABLE_WINDOWS_ACCESS="1"
export PATH="$PATH:/mnt/c/Program\ Files/Oracle/VirtualBox"
export VAGRANT_WSL_WINDOWS_ACCESS_USER_HOME_PATH="/mnt/c/Users/ihanm/Documents/Vagrant"

03. lincar usando
ln -s /mnt/c/Users/ihanm/Documents/Vagrant Vagrant

04. aplicar lab:
Vagrant.configure("2") do |config|

  def define_vm(config, name, box, manager)
    config.vm.define name do |vm|
      vm.vm.box = box
      # vm.vm.network "public_network", bridge: "Realtek PCIe GbE Family Controller" #Notebook Lenovo LOQ RTX4060
      vm.vm.provision 'shell', inline: <<-SHELL
        sudo #{manager} -y #{manager == 'zypper' ? 'refresh' : 'update'}
        sudo #{manager} -y install openssh-server
      SHELL
    end
  end

  # Distros:
  vms = [
    {name: 'WEB01', box: 'ubuntu/trusty64', manager: 'apt-get'},
    {name: 'DBA01', box: 'ubuntu/trusty64', manager: 'apt-get'},
    {name: 'WEB02', box: 'generic/rhel7', manager: 'yum'},
    {name: 'DBA02', box: 'generic/rhel7', manager: 'yum'},
    {name: 'WEB03', box: 'generic/opensuse42', manager: 'zypper'},
    {name: 'DBA03', box: 'generic/opensuse42', manager: 'zypper'}
  ]

  vms.each do |vm|
    define_vm(config, vm[:name], vm[:box], vm[:manager])
  end
end
